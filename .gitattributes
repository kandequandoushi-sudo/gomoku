# 默认让 Git 自动识别文本与二进制
* text=auto

# 源码与文档统一使用 LF 换行
*.py    text eol=lf
*.md    text eol=lf
*.txt   text eol=lf
*.toml  text eol=lf

# Windows 批处理脚本使用 CRLF
*.bat   text eol=crlf

# 引擎与模型为二进制，禁止换行符转换与文本 diff
*.exe   binary
*.bin   binary
*.lz4   binary
